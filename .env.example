# Road Sign Detection - Environment Variables
# Copy to .env and adjust as needed (optional)

# Dataset path for training (folder with class subdirs 0/, 1/, 2/, ...)
# DATASET_PATH=./dataset/Train

# Max upload size in bytes (default: 209715200 = 200MB)
# MAX_UPLOAD_SIZE=209715200
